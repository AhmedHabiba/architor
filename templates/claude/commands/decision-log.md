Read `.arch/decisions.md` and display the full decision log.

If the file doesn't exist or is empty, say: "No decisions recorded yet. Decisions are logged as you progress through the phases."

If the file exists, display it formatted clearly, organized by phase.

If $ARGUMENTS contains a filter (e.g., "phase 2", "security", "database"), filter the decisions to show only matching entries.

$ARGUMENTS
